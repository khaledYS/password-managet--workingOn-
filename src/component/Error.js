const Error = (props) => {
    return ( 
        <>
            <strong className="fjsakhfsfgsfg" style={{margin: props.margin, color:props.color}}>* {props.title} *</strong>
        </>
     );
}
 
export default Error;